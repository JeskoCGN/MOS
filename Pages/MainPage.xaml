<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:MOS.PageModels"             
             xmlns:models="clr-namespace:MOS.Models"
             xmlns:controls="clr-namespace:MOS.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:scanner="clr-namespace:BarcodeScanning;assembly=BarcodeScanning.Native.Maui"
             x:Class="MOS.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage">
    
    <ContentPage.BindingContext>
        <pageModels:MainPageModel/>
    </ContentPage.BindingContext>

    
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Label x:Name="Log" Grid.Row="0" Grid.Column="0" Text="{Binding Log}"></Label>

        <Button Grid.Row="1" Grid.Column="0" Text="Add" Command="{Binding ButtonClick_TestButton}"></Button>
    </Grid>

    
</ContentPage>
