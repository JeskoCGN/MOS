<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:MOS.PageModels"             
             xmlns:models="clr-namespace:MOS.Models"
             xmlns:controls="clr-namespace:MOS.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MOS.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage">
    
    <ContentPage.BindingContext>
        <pageModels:MainPageModel/>
    </ContentPage.BindingContext>

    
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Label x:Name="GTIN_Label" Grid.Row="0" Grid.Column="0" Text="Global Trade Item Number"></Label>
        <Label x:Name="GTIN_Output" Grid.Row="1" Grid.Column="0" Text="{Binding GS1Asset.GTIN}" TextColor="Black"></Label>
        <Label x:Name="ExpDate_Label" Grid.Row="2" Grid.Column="0" Text="Expiration Date"></Label>
        <Label x:Name="ExpDate_Output" Grid.Row="3" Grid.Column="0" Text="{Binding GS1Asset.ExpDate}" TextColor="Black"></Label>
        <Label x:Name="LOT_Label" Grid.Row="4" Grid.Column="0" Text="LOT"></Label>
        <Label x:Name="LOT_Output" Grid.Row="5" Grid.Column="0" Text="{Binding GS1Asset.LOT}" TextColor="Black"></Label>

        <Button Grid.Row="7" Grid.Column="0" Text="Add" Command="{Binding ButtonClick_TestButton}"></Button>
    </Grid>

    
</ContentPage>
